<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PdaPatrol" parent="Theme.AppCompat.Light.NoActionBar">

    </style>

    <style name="InputDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowSoftInputMode">stateAlwaysVisible|adjustPan</item>
    </style>

    <declare-styleable name="ConstraintHeightView">
        <attr name="maxHeight" format="dimension"/>
    </declare-styleable>

    <style name="popupwindow">
        <item name="android:windowEnterAnimation">@anim/popup_window_enter</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/popup_window_exit</item>
        <!-- 指定消失的动画xml -->
    </style>
</resources>